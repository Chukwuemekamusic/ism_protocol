{
  "name": "ism-protocol-subgraph",
  "version": "0.0.1",
  "description": "ISM Protocol subgraph for indexing lending pools, positions, auctions, and liquidations",
  "repository": "https://github.com/your-repo/ism-protocol",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio ism-protocol",
    "create-local": "graph create --node http://localhost:8020/ ism-protocol",
    "remove-local": "graph remove --node http://localhost:8020/ ism-protocol",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ism-protocol",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.35.1"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
